(Test/describe "Boolean" (fn ()
  (Test/describe "boolean value" (fn ()
    (Test/describe "true" (fn ()
      (Test/it "should be true" (fn ()
        (Test/expect true (Test/to-be-true))
      ))
    ))

    (Test/describe "false" (fn ()
      (Test/it "should be false" (fn ()
        (Test/expect false (Test/to-be-false))
      ))
    ))
  ))
))
